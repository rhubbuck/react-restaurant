import React from 'react';
import { MenuSectionContainer } from './MenuSectionElements'

const MenuSection = () => {
    return (
        <MenuSectionContainer>

        </MenuSectionContainer>
    )
}

export default MenuSection
